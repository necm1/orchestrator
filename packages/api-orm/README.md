## Building

Run `nx build api-orm` to build the library.

## Running unit tests

Run `nx test api-orm` to execute the unit tests via [Jest](https://jestjs.io).

## Migrations

-   Create Migration: `npm run orm:migrate --name=MigratonName`
-   Run Migrations: `yarn orm:run`
