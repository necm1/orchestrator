# shared-gql

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-gql` to build the library.

## Running unit tests

Run `nx test shared-gql` to execute the unit tests via [Jest](https://jestjs.io).
